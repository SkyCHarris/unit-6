

const person = {
    name: "Cynthia",
    city: "New York",
    age: 25,
    isStudent: true,
    skills: ['JavaScript', 'HTML', 'CSS'],
}

const message = `Hi, I'm ${person.name}. I live in ${person.city}.`
console.log(message);